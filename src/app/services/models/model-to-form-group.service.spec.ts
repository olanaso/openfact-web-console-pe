/* tslint:disable:no-unused-variable */

/*import { addProviders, async, inject } from '@angular/core/testing';
import { ModelToFormGroupService } from './model-to-form-group.service';

describe('Service: ModelToFormGroup', () => {
  beforeEach(() => {
    addProviders([ModelToFormGroupService]);
  });

  it('should ...',
    inject([ModelToFormGroupService],
      (service: ModelToFormGroupService) => {
        expect(service).toBeTruthy();
      }));
});*/
