/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CreateDebitnoteComponent } from './create-debitnote.component';

describe('Component: CreateDebitnote', () => {
  it('should create an instance', () => {
    let component = new CreateDebitnoteComponent();
    expect(component).toBeTruthy();
  });
});
