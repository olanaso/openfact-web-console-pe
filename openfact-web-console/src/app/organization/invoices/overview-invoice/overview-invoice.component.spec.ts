/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OverviewInvoiceComponent } from './overview-invoice.component';

describe('Component: OverviewInvoice', () => {
  it('should create an instance', () => {
    let component = new OverviewInvoiceComponent();
    expect(component).toBeTruthy();
  });
});
