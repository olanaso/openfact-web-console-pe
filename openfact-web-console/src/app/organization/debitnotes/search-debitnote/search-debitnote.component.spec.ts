/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SearchDebitnoteComponent } from './search-debitnote.component';

describe('Component: SearchDebitnote', () => {
  it('should create an instance', () => {
    let component = new SearchDebitnoteComponent();
    expect(component).toBeTruthy();
  });
});
