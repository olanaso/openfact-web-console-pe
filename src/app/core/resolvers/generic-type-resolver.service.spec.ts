/* tslint:disable:no-unused-variable */

/*import { TestBed, async, inject } from '@angular/core/testing';

import { GenericTypeResolverService } from './generic-type-resolver.service';

describe('GenericTypeService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [GenericTypeResolverService]
    });
  });

  it('should ...', inject([GenericTypeResolverService], (service: GenericTypeResolverService) => {
    expect(service).toBeTruthy();
  }));
});*/
