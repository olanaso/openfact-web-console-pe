/* tslint:disable:no-unused-variable */

/*import { addProviders, async, inject } from '@angular/core/testing';
import {TimeUnit} from './time-unit';

describe('TimeUnit', () => {
  it('should create an instance', () => {
    expect(new TimeUnit()).toBeTruthy();
  });
});*/
