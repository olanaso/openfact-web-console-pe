/**
 * Created by AHREN on 11/08/2016.
 */
/*
 tslint:disable:no-unused-variable 

import { addProviders, async, inject } from '@angular/core/testing';
 import { CertifiedProviderService } from './certified-provider.service';

 describe('Service: CertifiedService', () => {
 beforeEach(() => {
 addProviders([CertifiedProviderService]);
 });

 it('should ...',
 inject([CertifiedProviderService],
 (service: CertifiedProviderService) => {
 expect(service).toBeTruthy();
 }));
 });
*/
