window.OpenfactUIEnv = {
  "ssoApiUrl": "{{ .Env.OPENFACT_SSO_API_URL }}",
  "ssoRealm": "{{ .Env.OPENFACT_SSO_REALM }}",
  "ssoClientId": "{{ .Env.OPENFACT_SSO_CLIENT_ID }}",
  "openfactApiUrl": "{{ .Env.OPENFACT_API_URL }}"
};
