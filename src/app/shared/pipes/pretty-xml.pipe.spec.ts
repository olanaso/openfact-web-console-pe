import { PrettyXmlPipe } from './pretty-xml.pipe';

describe('PrettyXmlPipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyXmlPipe();
    expect(pipe).toBeTruthy();
  });
});
