/* tslint:disable:no-unused-variable */

/*import { addProviders, async, inject } from '@angular/core/testing';
import { OrganizationProviderService } from './organization-provider.service';

describe('Service: OrganizationProvider', () => {
  beforeEach(() => {
    addProviders([OrganizationProviderService]);
  });

  it('should ...',
    inject([OrganizationProviderService],
      (service: OrganizationProviderService) => {
        expect(service).toBeTruthy();
      }));
});*/
